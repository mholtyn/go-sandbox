TODO:

1. Struktura folderów
2. `go.mod` z zależnościami
3. Setup Echo w `main.go` (serwer, port, middleware)
4. Połączenie z MySQL (`database/sql` + driver)
5. Model `Task` (struct)
6. Stworzenie tabeli w MySQL (ręcznie lub migration SQL)
7. Handler GET /tasks — lista wszystkich
8. Handler POST /tasks — tworzenie
9. Handler GET /tasks/:id — pojedyncze zadanie
10. Handler PUT /tasks/:id — aktualizacja
11. Handler DELETE /tasks/:id — usunięcie